import { render, screen } from "@testing-library/react";
import SankeyChart from "../index";
import { sankeyChartData } from "../../../utils/staticData";

test("Sankey chart should be rendered", () => {
  // render(<SankeyChart data={sankeyChartData} />);
  // const sankeyChart = screen.getByTestId(/sankeyChart/i);
  // expect(sankeyChart).toBeInTheDocument();
});
